# --- Imágenes de disco y volcados ---
# Evita subir por accidente un ISO de varios GB dejado en la carpeta
*.iso
*.img
*.bin
*.raw
*.vhd
*.vhdx

# --- Salida de la consola y registros ---
*.log
*.txt.log
registro-*.txt

# --- Compilados (ps2exe) ---
*.exe
*.dll

# --- Configuración local con datos propios ---
# config.json SÍ se versiona (viene vacío, es la plantilla).
# preferencias.json NO: lo escribe el programa y lleva los numeros de
# serie de los discos de quien lo usa.
preferencias.json
config.local.json

# --- Windows ---
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
Desktop.ini
desktop.ini
$RECYCLE.BIN/
*.lnk

# --- Editores ---
.vs/
.vscode/
.idea/
*.swp
*~
